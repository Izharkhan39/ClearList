class Task {
  constructor(title, desc, dueDate, note, checklist) {
    this.title = title;
    this.desc = desc;
    this.dueDate = dueDate;
    this.note = note;
    this.checklist = checklist;
  }
}

export { Task };
